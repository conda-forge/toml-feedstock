{% set name = "toml" %}
{% set version = "0.9.6" %}
{% set sha256 = "380178cde50a6a79f9d2cf6f42a62a5174febe5eea4126fe4038785f1d888d42" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256|lower }}

build:
  number: 0
  noarch: python
  script: python -m pip install --no-deps --ignore-installed .

requirements:
  build:
    - python
    - pip

  run:
    - python

test:
  imports:
    - toml
  commands:
    - conda inspect linkages -p $PREFIX toml  # [not win]
    - conda inspect objects -p $PREFIX toml   # [osx]

about:
  home: https://github.com/uiri/toml
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: 'Python lib for TOML.'

extra:
  recipe-maintainers:
    - rlaverde
    - holgern
